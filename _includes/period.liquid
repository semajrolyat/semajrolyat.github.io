<div class="col">
  <a href="{% if period.redirect %}{{ period.redirect }}{% else %}{{ period.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if period.img %}
        {%
          include figure.liquid
          loading="eager"
          path=period.img
          sizes = "250px"
          alt="period thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ period.title }}</h2>
        <p class="card-text">{{ period.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if period.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ period.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if period.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ period.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
